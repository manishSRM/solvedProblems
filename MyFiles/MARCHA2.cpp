#include <cstdio>
#include <vector>
#include <algorithm>

using namespace std;

int main () {
	int T, N;
	scanf ("%d", &T);
	while (T--) {
		scanf ("%d", &N);
		vector <int> levels (N + 1);
		for (int i = 1; i <= N; i++) 
			scanf ("%d", &levels[i]);
		
	}
	return 0;
}